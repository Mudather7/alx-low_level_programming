#include <stdio.h>
/**
 * main - Entry point.
 *
 * @argc: is a count of the arguments.
 *
 * @argv: an array of pointers to the strings.
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
