#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x = 48;
	int n = 48;

	while (x = 48i, n <= 57)
	{
		while (x <= 57)
		{
			if (n != x && n < x)
			{
				putchar(n);
				putchar(x);

				if (n != 56 || x != 57)
				{
					putchar(',');
					putchar(' ');
				}
			}
			x++;
		}
		n++;
	}
	putchar('\n');
	return (0);
}
